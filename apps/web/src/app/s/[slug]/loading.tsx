/** A quiet placeholder while the catalogue loads; the page paints something immediately. */
export default function ShopLoading() {
  return (
    <main className="rk-container" style={{ padding: '4rem 0', textAlign: 'center' }}>
      <p className="rk-fineprint">Opening the shop…</p>
    </main>
  );
}
